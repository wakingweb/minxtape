<audio controls preload="auto" class="minxtape-track" id="minxtape-track-<%= track.position %>" data-position="<%= track.position %>" style="<%= 'display: none;' if track.position > 1 %>">
  <source src="<%= track.audio.url %>" type="audio/mpeg">
    Your browser does not support the audio element.
</audio>
